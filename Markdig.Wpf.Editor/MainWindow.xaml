<Window x:Class="Markdig.Wpf.Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Markdig.Wpf.Editor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <local:MarkdownEditor Background="Bisque" 
                              EditorBackground="LightGray" 
                              AutoUpdate="{Binding ElementName=AutoUpdateCheckBox, Path=IsChecked}"
                              IsEnabled="{Binding ElementName=IsEnabledCheckBox, Path=IsChecked}"
                              />
        <StackPanel Grid.Column="1" Margin="5">
            <Label Content="Control Panel:" Margin="5" FontWeight="Bold"></Label>
            <CheckBox Content="IsEnabled" x:Name="IsEnabledCheckBox" IsChecked="True"/>
            <CheckBox Content="AutoUpdate" x:Name="AutoUpdateCheckBox" IsChecked="True"/>
        </StackPanel>
    </Grid>
</Window>
